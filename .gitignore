# Fine-tuned model weights — regenerate with src/train_intent_classifier.py
# (265MB, over GitHub's file size limit; not something you'd hand-edit anyway)
models/

# Python
__pycache__/
*.pyc
.venv/
venv/

# The submitted report (including the AI-use declaration) stays local only,
# not on the public repo.
report/

# OS/editor cruft
.DS_Store
Thumbs.db
